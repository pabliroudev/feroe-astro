---
const { title, body, href, cta = "Ver más" } = Astro.props;
---
<div class="card info">
  <h3>{title}</h3>
  <p class="muted">{body}</p>
  {href && <a class="btn ghost" href={href}>{cta}</a>}
</div>

<style>
  .info h3{ margin: 0 0 8px; letter-spacing: -0.01em; }
  .info p{ margin: 0 0 14px; }
  .info .btn{ width: fit-content; }
</style>
